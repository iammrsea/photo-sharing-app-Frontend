import React from 'react';
import { GoogleLoginButton } from 'react-social-login-buttons';
export default () => {
	return (
		<GoogleLoginButton style={{ fontFamily: 'cursive', marginBottom: 10 }}>Sign In with Google</GoogleLoginButton>
	);
};
