import React from 'react';
import { GithubLoginButton } from 'react-social-login-buttons';
export default () => {
	return (
		<GithubLoginButton style={{ fontFamily: 'cursive', marginBottom: 10 }}>Sign In with Github</GithubLoginButton>
	);
};
